# Variáveis de Ambiente do Betengine
# Copie este arquivo para .env e preencha com seus valores

# Ambiente (development ou production)
NODE_ENV=production

# Porta (deixe como 3000, ou mude conforme sua hospedagem)
PORT=3000

# JWT Secret (MUDE ISSO PARA ALGO SEGURO!)
# Gere um valor aleatório forte com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=seu-secreto-super-seguro-aqui-MUDE-ISSO

# Google Gemini API Key (opcional, deixe em branco se não usar)
GEMINI_API_KEY=

# Database (deixe assim, vai usar SQLite local)
DATABASE_PATH=database.sqlite

# APP_URL (sua URL de produção, ex: https://seu-dominio.com)
APP_URL=https://seu-dominio.com
